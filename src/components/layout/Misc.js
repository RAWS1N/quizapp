// function handleClick(answer) {
    //     setOptions(prevOptions => prevOptions.map(element => {
    //         return answer === item.correctAnswer ? {...element,Class:'correct',isHeld:true} 
    //         :element.answer !== item.correctAnswer ? {...element,Class:'inCorrect'}
    //         : element.isHeld ? {...element,Class:"held"} :element
    //     }))
    // }

    // function handleClick(id){
    //     setOptions(prevOptions => prevOptions.map(element => {
    //         return element.id === id && element.answer === item.correctAnswer ? {...element,Class:"correct",isHeld:!element.isHeld} : element.id === id && element.answer !== item.correctAnswer ? {...element,Class:"inCorrect",isHeld:!element.isHeld} : {...element,Class:'notSelected'}
    //     }))
    // }
